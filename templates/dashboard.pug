extends layout

block content
	h2 My Polls
	.dashboard.list-group
		for poll in polls
			.list-group-item
				a(href="/polls/" + poll.id)= poll.name
				h4.pull-right
					a.label.label-danger(href="/polls/" + poll.id + "/delete") Delete
				.clearfix
